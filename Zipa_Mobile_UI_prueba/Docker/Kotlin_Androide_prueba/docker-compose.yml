version: "3.8"
services:
  api:
    build: ./api
    expose:
      - "5000"

  gateway:
    image: nginx:stable
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api

